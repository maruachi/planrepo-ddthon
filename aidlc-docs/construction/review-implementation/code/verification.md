# U3 검증 결과

2026-09-09. 최종 전체 실행은 23개 파일/86개 테스트 통과다. U3 추가 테스트 20개: 서비스 5, 화면 상태 8, 단위 통합/마이그레이션 4, 실제 HTTP 3. 타입 검사와 Vite/Node 빌드도 통과했다.

검증한 동작: 역할·댓글·소속 검사, 정확한 과거 리뷰 대상과 최신 관계, 요청 후 단계 이동/편집 뒤 결과 기록, 리뷰가 계획 state/revision을 변경하지 않음, 리뷰 중 자체 승인·다음 진행, 피어 결과가 자체 승인을 대신하지 않음, 리뷰 이력의 다음 CLI 문맥 연결, 결과 중복 거절, receipt 재조회, 페이지 목록, 수동 완료와 오래된 revision/역할 거절, 자동 CLI 호출 없음.

v2→v3 테스트는 기존 10개 테이블의 원문·실행 입력/결과·버전·결정·receipt를 비교해 보존을 확인했다. 기존 v1 마이그레이션 테스트는 최종 schema 기대값을 2에서 3으로 갱신했다. 첫 전체 실행의 유일한 실패는 이 이전 기대값이었으며 수정 후 최종 전체 86개가 통과했다.

실제 브라우저 통합은 U2의 실제 CLI 생성 문서가 있는 임시 v2 DB를 SQLite backup으로 복사해 v3 앱에서 수행했다. 첫 문서는 실제 인증 CLI 결과이고 이후 단계 생성은 결정적 테스트 어댑터를 사용했다. 9개 단계 모두를 실제 CLI로 실행했다는 주장은 하지 않는다.

확인한 화면 흐름:

- 실제 생성 문서 열람·승인 → Inception 문서 리뷰 요청 → 대기 중 다음 단계 진행.
- 요청 후 원래 문서 편집 → 리뷰어 전환 → 원래 버전 수정 요청, 응답 유실 후 정확한 receipt 확인.
- 리뷰어의 원래/최신 문서 비교와 자체 승인 분리, 390px 가로 넘침 없음.
- 역할 변경 취소 시 작성자 초안 유지.
- 나머지 계획 검토 → 구현 대기 → 키보드 수동 완료 확인, 추가 CLI 호출 없음.
- 앱 정상 종료·같은 DB 재시작 → 실제 생성 문서/리뷰 원래 대상/수동 완료 카드 보존.

독립 Chrome for Testing의 pageerror는 0개. 결과/화면은 /var/folders/sl/_cj854550hz43z0tzd13kjw00000gn/T/planrepo-final-browser-iLY8OP의 results.json, review-desktop.png, review-mobile.png, completed-board.png에 있다. 모바일 및 완료 보드 스크린샷을 직접 확인했다. 임시 증거는 OS가 정리할 수 있다.

활성 확장 차단 항목은 없으며 Security/Resiliency/PBT 모두 비활성/N/A. 운영 부하·다중 사용자·클라우드 배포는 범위 밖이다.
