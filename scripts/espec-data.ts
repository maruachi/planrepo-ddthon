/** eSPEC 테스트 데이터 (부작용 없는 데이터 전용 모듈 — 여러 시드 스크립트가 공유). */
import type { PlanningQuestion } from '../src/shared/planning-contracts.js';

export const TITLE = 'eSPEC 첨부파일 업로드 오류 조회/재처리 기능';

// 요구사항 확인 질문. options 에는 A/B/C 보기만 넣는다 — "X) 기타(직접 입력)"는 모달이 자동으로 제공한다.
export const QUESTIONS: PlanningQuestion[] = [
  { id: 'source-code', prompt: 'plan.md 는 기존 코드베이스(dks-espec-portal / Vue.js, dks-espec-endpoint / Spring Boot)를 수정·재사용하는 전제인데, 이 워크스페이스에는 해당 소스가 없습니다. 코드를 어떻게 제공하시려는 게 맞나요?', options: [
    '기존 코드베이스를 이 워크스페이스로 복사·체크아웃해서 제공한다 (권장 — 실제 코드 기반 재사용 설계 가능)',
    '코드 없이 plan.md 설명만으로 신규 스캐폴드를 생성한다 (기존 시스템 통합은 이후 직접 맞춘다)',
    '코드는 없지만 기존 구조(패키지 경로·Service 시그니처·인증 방식)를 텍스트로 알려준다',
  ] },
  { id: 'retry-granularity', prompt: '재처리를 어떤 단위로 하시려는 게 맞나요? (API 는 파일 1건 단위로 보이지만 화면 흐름상 다건 선택 가능성도 있습니다)', options: [
    '파일 1건 단위 재처리만 (파일 행마다 재처리 버튼)',
    '파일 다건 일괄 재처리 (체크박스 다중 선택 후 한 번에)',
    '문서(RPT_DOC) 단위 재처리 (해당 문서의 모든 오류 파일 일괄)',
  ] },
  { id: 'authorization', prompt: '"운영 권한 사용자만 재처리 허용"에서, 운영 권한을 시스템에서 어떻게 판별하려는 게 맞나요?', options: [
    '특정 역할(Role) 코드로 판별한다 (예: 운영자 역할 — 역할명은 직접 입력)',
    '특정 메뉴·기능 권한(Permission) 보유 여부로 판별한다',
    '조회는 모든 로그인 사용자 허용, 재처리(POST)만 운영 권한으로 제한한다',
  ] },
  { id: 'query-bounding', prompt: '운영 DB 부하 방지를 위해 조회 조건을 얼마나 강제하려는 게 맞나요?', options: [
    '등록일자(기간) 입력을 필수로 하고 페이징을 적용한다',
    '조건은 선택이되 조회 기간 최대 범위를 제한하고(예: 3개월) 페이징을 적용한다',
    '조건 없이 전체 조회를 허용하고 페이징만 적용한다 (기간 제한 없음)',
  ] },
  { id: 'source-file-access', prompt: 'FILE_UPLOAD_DTTM 이 비어있는 오류 건의 "원본"을 재처리 때 어디에서 다시 읽어오려는 게 맞나요?', options: [
    '임시·스테이징 저장소에 남은 원본 경로에서 다시 업로드 처리한다',
    '원본은 최종 저장소에 있고 메타(FILE_UPLOAD_DTTM)만 누락된 것이라, 파일 재전송 없이 후처리만 재수행한다',
    '원본 위치가 케이스마다 달라, 재처리 전 원본 존재 여부를 확인하고 없으면 실패 처리한다',
  ] },
  { id: 'concurrency', prompt: '동일 파일 동시 재처리(Race condition)를 어떻게 제어하려는 게 맞나요?', options: [
    '재처리 직전 현재 상태(FILE_UPLOAD_DTTM)를 재확인하고 이미 처리된 건은 Skip 한다 (추가 컬럼 없이 최소 구현)',
    '처리중 상태값(상태 컬럼 추가·활용)으로 락을 걸어 동시 요청 중 하나만 진행한다',
    'DB 행 잠금(SELECT ... FOR UPDATE)으로 엄격하게 제어한다',
  ] },
  { id: 'audit-trail', prompt: '재처리 성공·실패 이력을 어떻게 남기려는 게 맞나요?', options: [
    '애플리케이션 로그에만 성공·실패와 원인을 기록한다 (DB 이력 테이블 미생성 — plan.md 의 "신규 테이블 없음" 유지)',
    '별도 이력 테이블에 재처리 실행자·시각·결과를 저장한다 (신규 테이블 허용)',
    '로그 + 기존 첨부파일 레코드의 갱신 시각·처리자 컬럼 업데이트로 남긴다',
  ] },
  { id: 'ext-security', prompt: '이 프로젝트에 보안 확장(Security Baseline)을 적용하려는 게 맞나요?', options: [
    '예 — 모든 SECURITY 규칙을 필수(차단) 제약으로 적용한다 (운영급 애플리케이션 권장)',
    '아니오 — SECURITY 규칙을 적용하지 않는다 (PoC·프로토타입·실험용에 적합)',
  ] },
  { id: 'ext-resiliency', prompt: '이 프로젝트에 복원력 기준(Resiliency Baseline)을 설계 지침으로 적용하려는 게 맞나요?', options: [
    '예 — 복원력 기준을 방향성 있는 모범 사례·설계 지침으로 적용한다 (비즈니스 크리티컬 워크로드 권장)',
    '아니오 — 복원력 기준을 적용하지 않는다 (빠른 반복이 중요한 PoC·프로토타입에 적합)',
  ] },
  { id: 'ext-pbt', prompt: '이 프로젝트에 속성 기반 테스트(PBT) 규칙을 적용하려는 게 맞나요?', options: [
    '예 — 모든 PBT 규칙을 필수(차단) 제약으로 적용한다 (비즈니스 로직·데이터 변환·직렬화·상태 컴포넌트 권장)',
    '부분 적용 — 순수 함수와 직렬화 왕복(round-trip)에 대해서만 PBT 를 적용한다',
    '아니오 — PBT 규칙을 적용하지 않는다 (단순 CRUD·UI 위주·얇은 통합 계층에 적합)',
  ] },
];
