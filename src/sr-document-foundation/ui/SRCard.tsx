import { Link } from 'react-router-dom';
import type { SRSummary } from '../../shared/contracts.js';
export function SRCard({ sr }: { sr: SRSummary }) { return <Link className="sr-card" data-testid="sr-card-detail-link" to={`/srs/${sr.id}`}><span className="card-id">SR · {sr.id.slice(0, 8)}</span><h3>{sr.title}</h3>{sr.column === 'inception' && <span className="badge">Inception {sr.inceptionCycle ?? 0}회차</span>}{sr.column === 'construction' && <span className="badge">Construction {sr.constructionCycle ?? 0}회차</span>}{sr.planningStatus === 'running' && <span className="badge">생성 중</span>}{sr.planningStatus === 'failed' && <span className="badge">생성 실패</span>}{!!sr.pendingReviews && <span className="badge" data-testid="sr-card-review-status">리뷰 대기 {sr.pendingReviews}건</span>}{sr.column === 'implemented' && <span className="badge">수동 완료</span>}<div className="card-bottom"><time>{new Date(sr.createdAt).toLocaleDateString('ko-KR')}</time><span aria-hidden="true">↗</span></div></Link>; }
